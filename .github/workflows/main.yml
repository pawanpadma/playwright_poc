name: Playwright Tests by File

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Playwright Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
        - testFile: playwright_automation/tests/example.spec.ts
          name: Test1
        - testFile: playwright_automation/tests/home.spec.ts
          name: Test2
        # - testFile: playwright_automation/tests/login.spec.ts
        #   name: Test3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install ./playwright_automation

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Debug test file path
        run: |
          echo "Running file: ${{ matrix.testFile }}"  

      - name: Run test file
        run: npx playwright test ${{ matrix.testFile }} --reporter=list,html,json,blob
        continue-on-error: true

      - name: List workspace contents
        run: |
         echo "Current directory:"
         pwd
         echo "--- Top-level files/folders ---"
         ls -la
         echo "--- .report folder (if it exists) ---"
         ls -R .report || echo ".report folder not found"

      - name: List report directory
        run: |
          echo "Checking contents of workspace..."
          ls -la 
          echo "--- playwright-report"
          ls -la playwright-report
          echo "--- test-results"
         
          ls -la test-results || echo "test-results directory not found"  

      # - name: Upload HTML report for ${{ matrix.name }}
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: report-${{ matrix.name }}
      #     path: playwright-report
      #     retention-days: 5
      - name: Debug blob-report contents
        run: |
          echo "Blob report contents:"
          ls -R blob-report || echo "blob-report missing!"

      - name: Upload raw report for ${{ matrix.name }}
        uses: actions/upload-artifact@v4
        with:
          name: report-${{ matrix.name }}
          path: blob-report/

  # merge-reports:
  #   name: Merge All Reports
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Download all reports
  #       uses: actions/download-artifact@v4
  #       with:
  #         path: all-reports

  #     # - name: Install dependencies
  #     #   run: npm ci

  #     - name: Install Playwright Browsers
  #       run: npx playwright install --with-deps

  #     - name: List merge directory
  #       run: |
  #         echo "Checking contents of workspace..."
  #         ls -la 
  #         # echo "--- playwright-report"
  #         # ls -la playwright-report
  #         # echo "--- test-results"
         
  #         # ls -la test-results || echo "test-results directory not found"  

  #     - name: Merge reports
  #       run: |
  #         mkdir merged-report
  #         npx playwright merge-reports --reporter html ./all-reports/**/
  #         mv playwright-report merged-report/

  #     - name: Upload merged HTML report
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: merged-html-report
  #         path: merged-report/

  merge-reports:
      runs-on: ubuntu-latest
      needs: test
      steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Download all blob reports
        uses: actions/download-artifact@v4
        with:
          path: all-reports
          merge-multiple: true # ZIPs will be saved here, one per matrix

      - name: Debug structure (before unzip)
        run: find all-reports

      - name: Extract all report ZIPs
        run: |
          mkdir extracted-reports
          for zip in all-reports/*.zip; do
          foldername=$(basename "$zip" .zip)
          mkdir "extracted-reports/$foldername"
          unzip -q "$zip" -d "extracted-reports/$foldername"
          done

      - name: Debug extracted structure
        run: find extracted-reports
             
      - name: Debug contents of report.jsonl files
        run: |
          echo "🧾 report.jsonl contents:"
          find extracted-reports -name report.jsonl -exec echo {} \; -exec cat {} \;
      
      - name: Merge reports into HTML
        run: |
          mkdir -p merged-report
      
          # Find folders containing report.jsonl
          echo "🔍 Searching for valid report folders..."
          mapfile -t report_dirs < <(find extracted-reports -type f -name "report.jsonl" -exec dirname {} \;)
      
          echo "📂 Merging reports from:"
          printf '%s\n' "${report_dirs[@]}"
      
          if [ "${#report_dirs[@]}" -eq 0 ]; then
            echo "❌ No valid reports found to merge."
            exit 1
          fi
      
          # Merge them
          npx playwright merge-reports --reporter html "${report_dirs[@]}"
      
          # Move final HTML report into a folder
          mv playwright-report merged-report
      

      - name: Upload merged HTML report
        uses: actions/upload-artifact@v4
        with:
          name: merged-html-report
          path: merged-report

